export interface COMPONENT_COLUMN {
  id: string;
  name: string;
  description: string;
  action: JSX.Element;
  createdAt: string;
  updatedAt: string;
}
export interface PREVIEW {
  updatedAt: string;
  id: string;
  name: string;
  description: string;
  createdAt: string;
}
